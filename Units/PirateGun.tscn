[gd_scene load_steps=5 format=3 uid="uid://bfud1ldvrk21k"]

[sub_resource type="GDScript" id="GDScript_45lqv"]
script/source = "extends CharacterBody2D

@export var side := 1.0

var health = 20
var damage = 10
var cooldown = false

@onready var hitbox := $Hitbox
const SPEED = 60.0

func _ready():
	if side == -1:
		$animation.flip_h = true
		hitbox.position.x = -160.005

func _physics_process(delta: float) -> void:
	velocity = Vector2.RIGHT * SPEED * side

	var overlapping_bodies = $Hitbox.get_overlapping_bodies()
	if overlapping_bodies.size() > 0:
		for body in overlapping_bodies:
			if body.side != side && !cooldown:
				attack(body)
				return
		
		if !cooldown:
			$animation.play(\"idle\")
		return 
	
	$animation.play(\"walk\")
	move_and_slide()
	
	
func get_hurt(damage):
	health -= damage
	if health <= 0:
		queue_free() # send money if side == -1

func attack(body):
	cooldown = true
	
	$animation.play(\"slash\")
	
	if body.has_method(\"get_hurt\"):
		body.get_hurt(damage)
		
	$AttackTimer.start()

func _on_attack_timer_timeout() -> void:
	cooldown = false
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_da46e"]
size = Vector2(39, 63)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_rfy7y"]
size = Vector2(221, 20)

[sub_resource type="SpriteFrames" id="SpriteFrames_gbcmk"]

[node name="PirateGun" type="CharacterBody2D"]
script = SubResource("GDScript_45lqv")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(15.5, -28.5)
shape = SubResource("RectangleShape2D_da46e")

[node name="Hitbox" type="Area2D" parent="."]
position = Vector2(41, -31)
rotation = -0.0028006176

[node name="CollisionShape2D" type="CollisionShape2D" parent="Hitbox"]
position = Vector2(107.5, 0)
shape = SubResource("RectangleShape2D_rfy7y")

[node name="AttackTimer" type="Timer" parent="."]
one_shot = true

[node name="animation" type="AnimatedSprite2D" parent="."]
position = Vector2(17, -25)
scale = Vector2(2, 2)
sprite_frames = SubResource("SpriteFrames_gbcmk")

[connection signal="timeout" from="AttackTimer" to="." method="_on_attack_timer_timeout"]
